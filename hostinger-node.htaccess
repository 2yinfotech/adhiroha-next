# ===================================================================
# Adhiroha — reverse-proxy .htaccess for the Next.js NODE app.
# Put this file in  public_html/.htaccess  (rename to ".htaccess").
# It forwards EVERY request to the running Node server (next start),
# which serves all pages, /api routes, redirects and DB-driven blogs.
# The Node app must be running (PM2 / Hostinger Node app) on port 3000.
# ===================================================================

RewriteEngine On

# --- Canonical host -------------------------------------------------
# Every canonical tag on the site points at www.adhiroha.com, but the apex
# also answered 200 with the full site, splitting crawl budget and inbound
# links across two hostnames. 301 the apex to www BEFORE the proxy rule,
# keeping path and query string ([R=301,L] carries the query by default).
RewriteCond %{HTTP_HOST} ^adhiroha\.com$ [NC]
RewriteRule ^(.*)$ https://www.adhiroha.com/$1 [L,R=301]

# Forward everything to the Node.js app on 127.0.0.1:3000
RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L]
